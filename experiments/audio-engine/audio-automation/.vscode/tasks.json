{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install (pip)",
      "type": "shell",
      "command": "python -m pip install -r requirements.txt",
      "problemMatcher": []
    },
    {
      "label": "Run pipeline (default)",
      "type": "shell",
      "command": "python -m src.orchestrate --playlist playlist.txt --config config.yaml",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
