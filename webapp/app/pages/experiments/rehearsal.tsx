export default function RehearsalPage() {
  return (
    <main className="min-h-screen bg-[var(--bg)] text-[var(--text)] p-6">
      <h1 className="text-xl font-semibold mb-2">Rehearsal</h1>
      <p className="text-sm text-[color:var(--muted)]">Project landing for the Rehearsal experiment. Wire your UI here or load the embedded module.</p>
    </main>
  );
}
