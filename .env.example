# Copy to .env and fill in your credentials
# Public or link-shared files can be fetched with an API key only.
# For private files, extend the adapter to use OAuth or a service account.

# Google Drive API Key
GDRIVE_API_KEY=
